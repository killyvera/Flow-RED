{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://redflow.dev/schemas/agent-core.react.schema.json",
  "title": "Agent Core Node Configuration (REACT Strategy)",
  "description": "Configuration schema for the Agent Core orchestrator node using REACT (Reason → Act) strategy",
  "type": "object",
  "required": ["strategy", "maxIterations", "allowedTools"],
  "properties": {
    "strategy": {
      "const": "react",
      "description": "Orchestration strategy - currently only REACT is supported"
    },
    "maxIterations": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20,
      "default": 5,
      "description": "Maximum number of REACT loop iterations before forcing termination"
    },
    "allowedTools": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "List of tool names that the agent is allowed to use. Must match connected tool node types."
    },
    "stopConditions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type"],
        "properties": {
          "type": {
            "enum": [
              "final_answer",
              "confidence_threshold",
              "iteration_limit"
            ],
            "description": "Type of stop condition to evaluate"
          },
          "value": {
            "description": "Condition-specific value (e.g., confidence threshold as number between 0-1)"
          }
        },
        "additionalProperties": false
      },
      "default": [],
      "description": "Conditions that will cause the agent loop to terminate early"
    },
    "debug": {
      "type": "boolean",
      "default": false,
      "description": "Enable detailed debug logging for observability"
    }
  },
  "additionalProperties": false
}

